CREATE TABLE sales (
    order_id INT,
    order_date DATE,
    customer_id INT,
    product VARCHAR(100),
    category VARCHAR(50),
    quantity INT,
    price NUMERIC,
    revenue NUMERIC,
    country VARCHAR(50)
);